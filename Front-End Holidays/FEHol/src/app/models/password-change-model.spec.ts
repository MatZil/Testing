import { PasswordChangeModel } from './password-change-model';

describe('PasswordChangeModel', () => {
  it('should create an instance', () => {
    expect(new PasswordChangeModel()).toBeTruthy();
  });
});
